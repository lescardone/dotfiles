- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/work

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      sterr: true

#   - [git submodule update --init --recursive, Installing submodules]
